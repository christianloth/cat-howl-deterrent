tensorflow>=2.16
tensorflow-hub>=0.16
sounddevice>=0.4.6
soundfile>=0.12
numpy>=1.24
librosa>=0.10
tqdm>=4.66
scikit-learn>=1.3

# Mac (Neural Engine) — install on Mac only:
# coremltools>=7.2

# Linux GPU (NVIDIA) — install on Linux only:
# tensorflow[and-cuda]>=2.16

# For training-time corpus pulls:
# datasets[audio]>=2.18
